export enum EAPIMethod {
  GET = 'GET',
  POST = 'POST',
  PUT = 'PUT',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}

export enum EProposalStatus {
  'ACCEPTED' = 0,
  'PENDING' = 1,
  'REJECTED' = 2,
}
